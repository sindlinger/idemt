{
  "name": "telnetmt-cmdmt",
  "version": "0.1.10",
  "type": "module",
  "bin": {
    "cmdmt": "dist/index.js"
  },
  "description": "TelnetMT CLI (socket) for MT5 service",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "validate-params": "tsx tools/validate-params.ts"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
